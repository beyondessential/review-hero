# Agent rules for Review Hero

## Pull requests

When creating a PR, always include the contents of `.github/pull_request_template.md` at the top of the PR body, followed by the rest of the description.
