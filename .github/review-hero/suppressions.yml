- pattern: "Missing try/catch around JSON.parse() in functions that load application state files"
  context: "When the developer has explicitly stated that the file source is guaranteed to be safe (e.g., internally generated, validated on write, or protected by application guarantees)"
  reason: "Developer indicated defensive error handling is unnecessary when the file integrity is guaranteed by application design or constraints, making the suggestion a false positive for their use case"
- pattern: "Missing HTTP response status check in fetch/API calls"
  context: "When flagging fetch() or HTTP client calls that lack response.ok validation"
  reason: "Developer indicated the external service (GitHub API) is reliable and failures are not a realistic concern in their operational context"
- pattern: "Missing nullish-coalescing operator (??) on object properties in arithmetic operations"
  context: "When the property is already used elsewhere in the codebase with ?? 0 or similar defensive coding"
  reason: "Developer rejected this as overly defensive given their data validation guarantees upstream; flagging inconsistent defensive patterns can be treated as style rather than correctness"
